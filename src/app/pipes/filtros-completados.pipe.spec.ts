import { FiltrosCompletadosPipe } from './filtros-completados.pipe';

describe('FiltrosCompletadosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrosCompletadosPipe();
    expect(pipe).toBeTruthy();
  });
});
